<%= form_for object, method: :patch do |f|%>
  <%= hidden_field_tag :request_extend, true %>
  <%= hidden_field_tag :user_id, object.user_id %>
  <%= hidden_field_tag :book_id, object.book_id %>
  <input type="number" name="extension_day">
  <%= f.submit "Extend", class: "btn btn-primary" %>
<% end %>